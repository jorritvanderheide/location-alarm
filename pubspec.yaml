name: location_alarm
description: Location-based alarm app
publish_to: "none"
version: 1.0.0+1
license: PolyForm-Shield-1.0.0

environment:
  sdk: ^3.11.0

dependencies:
  drift: ^2.32.0
  flutter:
    sdk: flutter
  flutter_map: ^8.2.2
  flutter_riverpod: ^3.3.1
  geolocator: ^14.0.2
  go_router: ^17.1.0
  latlong2: ^0.9.1
  path: ^1.9.1
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  sqlite3_flutter_libs: ^0.6.0+eol

dev_dependencies:
  build_runner: ^2.13.0
  drift_dev: ^2.32.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
